version: "3.4"

services:

  # nats:
  #   image: nats
  #   ports:
  #     - 4222:4222

  mysql:
    image: mysql
    restart: always
    ports:
      - 3307:3307
    environment:
      MYSQL_ROOT_PASSWORD: root_password
      MYSQL_DATABASE: nghia
      MYSQL_USER: test_user
      MYSQL_PASSWORD: test_user
      MYSQL_TCP_PORT: 3307
